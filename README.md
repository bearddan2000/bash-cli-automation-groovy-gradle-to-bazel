# bash-cli-automation-groovy-gradle-to-bazel

## Description
Conversion of groovy gradle build
to bazel build.

## Tech stack
- bash

## To run
`sudo ./install.sh -u`

## To stop
`sudo ./install.sh -d`

## For help
`sudo ./install.sh -h`
